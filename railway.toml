[build]
builder = "nixpacks"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# Railway Volume must be mounted at /data in your service settings.
# The bot reads/writes  /data/coupons.json  for all persistent storage.
